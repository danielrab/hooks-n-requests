{
  // If you change settings.json, call "Apply settings" from the menu.
  "watch-run.targetList": [
      {
          "target": "/src/**",
          "task": "compile ts"
      },
      {
        "target": "/assets/**",
        "task": "copy assets"
    }
  ]
}